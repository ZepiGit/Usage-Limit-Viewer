<?xml version="1.0" encoding="utf-8"?>
<!--
  Credentials never leave the device. Cloud backup and device-to-device transfer are
  fully disabled: Keystore-wrapped tokens are hardware-bound and would be useless
  after a restore, and the usage cache is re-derived on the next sync.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
